/** @type {import('next').NextConfig} */
const nextConfig = {
  // No Next.js 14, o App Router é estável e não precisa de experimental.appDir
  // A pasta src é detectada automaticamente quando existe
}

module.exports = nextConfig
